apiVersion: v1
kind: Service
metadata:
  name: {{ include "grow-chart.fullname" . }}
  labels:
    {{- include "grow-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grow-chart.selectorLabels" . | nindent 4 }}
