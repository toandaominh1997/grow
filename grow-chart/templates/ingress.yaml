apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grow-ingress 
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
spec:
  rules:
    - host: hello-world.info
      http:
        paths:
          # - path: /
          #   pathType: Prefix
          #   backend:
          #     service:
          #       name: nginx
          #       port:
          #         number: 80
          - path: /api 
            pathType: Prefix
            backend:
              service:
                name: fastapi 
                port:
                  number: 1234
